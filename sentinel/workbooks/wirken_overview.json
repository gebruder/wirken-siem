{
  "version": "Notebook/1.0",
  "items": [
    {
      "type": 1,
      "content": {
        "json": "## Wirken audit overview\n\nFive-detection overview against the Wirken 1.3.x audit DCRs. Each tile reads from `Custom_WirkenAudit_CL` (legacy) or `Custom_WirkenSession_CL` (typed).\n\nDetection 5 corresponds to a wirken code gap and is intentionally absent."
      },
      "name": "header"
    },
    {
      "type": 3,
      "content": {
        "version": "KqlItem/1.0",
        "query": "Custom_WirkenSession_CL\n| where Kind == 'assistant_tool_calls'\n| mv-expand call = parse_json(Event).calls\n| where tostring(call.name) == 'exec'\n| extend cmd = tostring(parse_json(tostring(call.arguments)).command)\n| where cmd matches regex @'(?i)^(/[^ ]+/)?(curl|wget|msiexec|invoke-webrequest|iwr|bitsadmin|certutil)\\b'\n| summarize count() by bin(TimeGenerated, 5m)",
        "size": 0,
        "title": "Detection 1: shell outbound fetch (count by 5m)",
        "queryType": 0,
        "resourceType": "microsoft.operationalinsights/workspaces",
        "visualization": "linechart"
      },
      "name": "d1"
    },
    {
      "type": 3,
      "content": {
        "version": "KqlItem/1.0",
        "query": "let calls = Custom_WirkenSession_CL | where Kind == 'assistant_tool_calls' | mv-expand call = parse_json(Event).calls | where tostring(call.name) == 'exec' | project CallTime = TimeGenerated, SessionId, CallId = tostring(call.id);\nlet results = Custom_WirkenSession_CL | where Kind == 'tool_result' | extend body = parse_json(Event) | where tostring(body.tool_name) == 'exec' | project ResultTime = TimeGenerated, SessionId, CallId = tostring(body.call_id);\ncalls\n| join kind=inner results on SessionId, CallId\n| extend DurationMs = datetime_diff('millisecond', ResultTime, CallTime)\n| where DurationMs > 30000\n| summarize count() by bin(ResultTime, 15m)",
        "size": 0,
        "title": "Detection 2: long-running exec (>30s, count by 15m)",
        "queryType": 0,
        "resourceType": "microsoft.operationalinsights/workspaces",
        "visualization": "linechart"
      },
      "name": "d2"
    },
    {
      "type": 3,
      "content": {
        "version": "KqlItem/1.0",
        "query": "Custom_WirkenSession_CL\n| where Kind == 'assistant_tool_calls'\n| mv-expand call = parse_json(Event).calls\n| where tostring(call.name) == 'write_file'\n| extend path = tostring(parse_json(tostring(call.arguments)).path)\n| where path matches regex @'(?i)\\.(exe|dll|so|dylib|msi|scr|bat|ps1|sh|cmd|com|vbs|jar)$'\n| summarize count() by bin(TimeGenerated, 15m)",
        "size": 0,
        "title": "Detection 3: binary write (count by 15m)",
        "queryType": 0,
        "resourceType": "microsoft.operationalinsights/workspaces",
        "visualization": "linechart"
      },
      "name": "d3"
    },
    {
      "type": 3,
      "content": {
        "version": "KqlItem/1.0",
        "query": "let prefixes = _GetWatchlist('wirken_skill_dirs') | project prefix;\nCustom_WirkenSession_CL\n| where Kind == 'assistant_tool_calls'\n| mv-expand call = parse_json(Event).calls\n| where tostring(call.name) == 'exec'\n| extend Argv = tostring(parse_json(tostring(call.arguments)).command)\n| extend MatchedPrefix = tostring(toscalar(prefixes | where Argv contains tostring(prefix) | take 1 | project prefix))\n| where isnotempty(MatchedPrefix)\n| summarize count() by bin(TimeGenerated, 15m)",
        "size": 0,
        "title": "Detection 4: skill-dir exec (count by 15m)",
        "queryType": 0,
        "resourceType": "microsoft.operationalinsights/workspaces",
        "visualization": "linechart"
      },
      "name": "d4"
    },
    {
      "type": 3,
      "content": {
        "version": "KqlItem/1.0",
        "query": "Custom_WirkenAudit_CL\n| where Action == 'audit.chain_broken'\n| summarize count() by bin(TimeGenerated, 5m)",
        "size": 0,
        "title": "Detection 6: chain-broken events (count by 5m)",
        "queryType": 0,
        "resourceType": "microsoft.operationalinsights/workspaces",
        "visualization": "linechart"
      },
      "name": "d6"
    }
  ],
  "$schema": "https://github.com/Microsoft/Application-Insights-Workbooks/blob/master/schema/workbook.json"
}
